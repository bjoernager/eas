        THUMB
        ARM
        CODE16
        CODE32
;        CODE64 ; Invalid.

;        ALIGN 32
;_start  PROC
;        MOV r0, pc
;        NOP                    ; This is a comment.
;        EOR r0, r0, r0         ; Þið sjáið snæri! (Note illegal characters).
;        ADD r0, r0, #0x1

        END
